id: script_node
namespace: examples.plugins
description: Run an inline Node.js script through the shared runner contract.
labels:
  team: platform
tasks:
  - id: hello
    type: script.node
    source:
      type: inline
      content: |
        console.log(`node:${process.argv[2]}`);
    args: [amesh]
