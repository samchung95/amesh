{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "amesh.serviceAccountName" . }}
  labels:
    {{- include "amesh.labels" . | nindent 4 }}
{{- end }}
