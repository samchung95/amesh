# coding: utf-8

"""
    AMESH

    Clean-room durable workflow MVP with validated flow management, execution control, webhook triggers and execution logs.

    The version of the OpenAPI document: 0.2.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from amesh_client.models.agent_route_assessment import AgentRouteAssessment
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class AgentRouteDecision(BaseModel):
    """
    AgentRouteDecision
    """ # noqa: E501
    assessments: List[AgentRouteAssessment]
    decision_digest: Annotated[str, Field(strict=True)] = Field(alias="decisionDigest")
    explanation: StrictStr
    factor_order: List[StrictStr] = Field(alias="factorOrder")
    ranked_member_ids: List[Annotated[str, Field(min_length=1, strict=True, max_length=128)]] = Field(alias="rankedMemberIds")
    required_capabilities: List[Annotated[str, Field(min_length=1, strict=True, max_length=128)]] = Field(alias="requiredCapabilities")
    schema_version: Optional[StrictStr] = Field(default='amesh.agent-route/v1', alias="schemaVersion")
    selected_agent: Annotated[str, Field(min_length=1, strict=True, max_length=128)] = Field(alias="selectedAgent")
    selected_agent_revision: StrictInt = Field(alias="selectedAgentRevision")
    selected_member_id: Annotated[str, Field(min_length=1, strict=True, max_length=128)] = Field(alias="selectedMemberId")
    selected_task: Annotated[str, Field(min_length=1, strict=True, max_length=128)] = Field(alias="selectedTask")
    __properties: ClassVar[List[str]] = ["assessments", "decisionDigest", "explanation", "factorOrder", "rankedMemberIds", "requiredCapabilities", "schemaVersion", "selectedAgent", "selectedAgentRevision", "selectedMemberId", "selectedTask"]

    @field_validator('decision_digest', mode="before")
    def decision_digest_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^sha256:[0-9a-f]{64}$", value):
            raise ValueError(r"must validate the regular expression /^sha256:[0-9a-f]{64}$/")
        return value

    @field_validator('schema_version')
    def schema_version_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['amesh.agent-route/v1']):
            raise ValueError("must be one of enum values ('amesh.agent-route/v1')")
        return value

    @field_validator('selected_agent', mode="before")
    def selected_agent_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^[A-Za-z0-9][A-Za-z0-9_-]*$", value):
            raise ValueError(r"must validate the regular expression /^[A-Za-z0-9][A-Za-z0-9_-]*$/")
        return value

    @field_validator('selected_member_id', mode="before")
    def selected_member_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^[A-Za-z0-9][A-Za-z0-9_-]*$", value):
            raise ValueError(r"must validate the regular expression /^[A-Za-z0-9][A-Za-z0-9_-]*$/")
        return value

    @field_validator('selected_task', mode="before")
    def selected_task_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^[A-Za-z0-9][A-Za-z0-9_-]*$", value):
            raise ValueError(r"must validate the regular expression /^[A-Za-z0-9][A-Za-z0-9_-]*$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AgentRouteDecision from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in assessments (list)
        _items = []
        if self.assessments:
            for _item_assessments in self.assessments:
                if _item_assessments:
                    _items.append(_item_assessments.to_dict())
            _dict['assessments'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AgentRouteDecision from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "assessments": [AgentRouteAssessment.from_dict(_item) for _item in obj["assessments"]] if obj.get("assessments") is not None else None,
            "decisionDigest": obj.get("decisionDigest"),
            "explanation": obj.get("explanation"),
            "factorOrder": obj.get("factorOrder"),
            "rankedMemberIds": obj.get("rankedMemberIds"),
            "requiredCapabilities": obj.get("requiredCapabilities"),
            "schemaVersion": obj.get("schemaVersion") if obj.get("schemaVersion") is not None else 'amesh.agent-route/v1',
            "selectedAgent": obj.get("selectedAgent"),
            "selectedAgentRevision": obj.get("selectedAgentRevision"),
            "selectedMemberId": obj.get("selectedMemberId"),
            "selectedTask": obj.get("selectedTask")
        })
        return _obj
